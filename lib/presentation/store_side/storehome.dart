import 'package:flutter/cupertino.dart';

class StoreHome extends StatelessWidget {
  const StoreHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('store');
  }
}
